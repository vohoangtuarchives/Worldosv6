[package]
name = "worldos-ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
worldos-core = { path = "../worldos-core" }
libc = "0.2"
rand = "0.8.5"
rayon = "1.10"
serde_json = "1.0.149"
worldos-rules = { version = "0.1.0", path = "../worldos-rules" }
