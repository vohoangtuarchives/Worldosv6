[package]
name = "worldos-rules"
version = "0.1.0"
edition = "2021"
description = "WorldOS Rule Engine (DSL parser and VM for simulation rules)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
rand = "0.8"
